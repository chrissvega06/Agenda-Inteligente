<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.agendainteligente">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".interfaz_cambiar_email"></activity>
        <activity android:name=".interfaz_cerrar_sesion" />
        <activity android:name=".interfaz_perfil" />
        <activity android:name=".interfaz_crear_nota" />
        <activity android:name=".interfaz_notas" />
        <activity android:name=".Dashboard" />
        <activity
            android:name=".interfaz_seguridad"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".interfaz_recuperar_password"
            android:theme="@style/Theme.AppCompat.NoActionBar" />
        <activity
            android:name=".interfaz_Registrar_usuario"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>